# Incrementally scan all configured Hermes cron outputs every day at 00:05.
# Adjust /opt/hermes-dashboard if the project is installed elsewhere.
5 0 * * * cd /opt/hermes-dashboard && set -a && . ./.env && set +a && /usr/bin/python3 scripts/scan_cron_outputs.py >> data/cron-scan.log 2>&1
